/** Shared native input/select classes for modals and forms (matches CreateMeetingModal). */
export const formInputClass =
  'block mt-1 w-full rounded-lg border border-border bg-background-card text-text-primary px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-primary';

/** Compact controls for inline inspector rows (task recurrence). */
export const formInputClassCompact =
  'rounded border border-border bg-background-card text-text-primary px-1.5 py-0.5 text-xs h-7 focus:outline-none focus:ring-1 focus:ring-primary';
