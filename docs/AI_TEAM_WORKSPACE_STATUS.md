# AI Team workspace — role pipelines

Primary surface: `/workspace/ai-team` manages **Planner → Worker → Reviewer** per role (Marketing, Product, Support, Engineering, Researcher), not single-persona chat.

See [`AI_ROLE_PIPELINES.md`](./AI_ROLE_PIPELINES.md) for the runtime contract, model registry, and diagrams.

Also linked: queue (`/workspace/ai-team/queue`), attention (`/workspace/ai-attention`), Admin model registry (`/admin/ai/models`).
