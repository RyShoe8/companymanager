# AI control-system implementation status

September 5, 2026. The original roadmap is unchanged.

## Delivered

- Versioned objective, draft, dependency, inference and run contracts.
- Planning page available to all organizations when globally enabled at `/workspace/projects/<project-id>/ai`, with manager/admin mutations and immutable drafts.
- Queued remote planning through Vercel cron: at most one inference request per invocation, global leases, idempotent submissions, transactional budget reservations and run events.
- Server-only bearer authentication, HTTPS-only gateway, bounded inputs/responses, deadlines and sanitized errors; no automatic inference retries.
- Authorization, objective digest, project snapshot and configuration rechecks. Expired attempts are blocked without replay; late responses are fenced.
- Cancellation, run summaries and token usage. Unknown charges retain reservations. In-flight cancellation suppresses drafts but cannot guarantee provider-side cancellation.
- Explicit approval transactionally appends up to 20 unassigned tasks, with digest/expiry/version checks and replay protection. Human assignments and task metadata are preserved.
- Additive index creation before queue writes. Narrow polling only while runs are pending and the page is visible/active.
- Database-backed Admin → AI Settings for connection, processing and per-request/monthly ceilings; only bearer/cron secrets remain in the environment. Manual planning defaults on for all organizations, while inference defaults off with zero budgets.
- Organization and project budget pages, parent-ceiling enforcement, exact USD input conversion, optimistic save revisions and transactional settings audit records. Queue admission fences settings edits; read-only settings loads do not update existing documents. Settings pages have no polling.
- Project AI run history (25 records/page) and an on-demand run inspector: usage, separate organization/project reservation records, plan approval evidence, input/policy digests and ordered events (50/page). History and manager-authorized cancellation remain available when planning is disabled; organization/project access checks still apply. Prompt text, credentials, worker lease tokens and organization-wide usage are not returned. Views replace pages rather than accumulating an unbounded cache and perform no periodic polling.
- Objective/plan history with 25 lightweight summaries per page, collection/project-bound cursors, on-demand full details, exact-version review and approval through the existing transaction. Older objectives can be explicitly selected for new planning without enlarging the recent-history cache beyond one selected item. Expired/stale drafts remain viewable but cannot bypass approval checks. Run evidence links directly to its original plan version.
- An on-demand **AI needs attention** workspace view for review-ready and blocked/failed runs, linked from the user menu. It reuses project contribution rules, returns at most 25 visible items, and scans at most 150 candidate runs per request. Continuation cursors are encrypted and bound to the user, organization, filter and expiry. Personal acknowledgments hide only that run revision, never change execution/approval state, and do not affect another user's list. Subsequent revisions reappear. The view has no polling; separate server-side email integration is described below.
- A narrow existing seen/activity timestamp boundary fix with a controlled-clock regression test.

Shared inference load limits are administrator-editable: 48 attempts per UTC day, 300 seconds between attempts and 2048 output tokens by default. Persisted global usage commits atomically with the dispatch marker, counts failed/ambiguous attempts, survives settings edits and preserves cooldown across midnight. Throttled jobs remain queued and cancellable. These bounds do not establish the remote host's actual capacity.

Admin AI Settings now includes a manually refreshed shared-usage snapshot: UTC day, recorded attempts, allowance remaining, last attempt and earliest eligibility under request limits. It uses an administrator-only no-store endpoint with a single indexed record lookup, no periodic polling, and no provider health/resource claims. This is a current allowance view, not historical metering or a provider invoice.

The same page includes read-only planning diagnostics: queue/running/expired-lease counts capped at 100+, oldest queued timestamp and unexpired dispatcher lease timestamp. Queries use minimal field projections, bounded results and three-second database query deadlines. No prompt text, identifiers or lease credentials are returned. Counts are observational, not a consistent transactional snapshot or confirmation of provider activity. Recovery remains owned by the existing scheduled worker.

Terminal-job context retention now covers queued cancellation as well as worker completion. The authenticated scheduled worker also clears at most 100 older inactive terminal jobs per invocation, including when inference is paused. An indexed cleanup marker makes maintenance resumable; write-time eligibility checks preserve queued/running context. Original objectives, plans, digests, jobs, events and budgets remain. Broader record/blob retention policy is still open.

No local model, persistent local worker, remote shell, sandbox or autonomous execution was enabled. Planning submits only selected objective fields, not repository files.

Organization/project manager pause controls now share the audited budget settings. Parent pauses override project controls; policy checks block new submissions and dispatch and reject returned drafts after a pause. Resuming does not reauthorize old queued policy revisions. This does not stop provider-side computation or remove previously created plans; manual planning, history and cancellation remain available.

Manager budget pages show current-month scoped settled spend, held reservations and remaining allowance under current saved ceilings. The indexed, projected ledger read is bounded to one record and does not create/reset a ledger. Missing activity is explicit. Monthly ledger history is available with 12 records per page and explicit refresh, scoped manager authorization and no polling. Historical recorded limits are distinguished from current ceilings. Immutable usage metering and provider invoice reconciliation remain open.

Planning completion now transactionally queues a generic review-ready or blocked update for the requesting manager when their workspace digest preference is enabled. The existing digest sender rechecks AI run revision/status, organization membership, manager role, project ownership and preference before delivery. Stale/revoked AI events are marked suppressed, not sent. No objective/project names, prompts or provider details are included in AI email rows. Delivery batches are limited to 100 events per recipient per invocation. Per-recipient six-minute leases fence overlapping scheduled senders and recheck opt-out before sending; ambiguous failures retain the lease until expiry. This reuses the existing email transport; retry after ambiguous email completion can still duplicate a digest and is not claimed exactly-once.

## Remaining gates

Scoped service identities and grants now have durable storage, transactional audits, server-side credential authentication and administrator UI/API controls. Registration starts disabled; credentials are shown once, hashed at rest, expire after 30 days and rotate explicitly. Grants bind one organization/project/run, operation, policy revision and validity window. Disablement epochs prevent reactivation from reviving old grants. Protected review writes recheck current authority, issuer, run state and stop controls transactionally. Only planning inference and artifact review are modeled; no production identities/grants or execution permissions were created. Lease-bound runtime integration remains open. See [service identity integration requirements](AI_SERVICE_IDENTITY_CONTRACT.md).

Exact-artifact storage, authenticated reviewer submission, project artifact/review screens and transactional human acceptance are implemented. Stored bytes are hashed and bounded; acceptance checks current manager/reviewer/grant authority, exact bytes, task version and policy, and atomically consumes one acceptance with task completion while preserving assignments. An on-demand plain-text patch/evidence viewer now verifies hashes and returns bounded pages without polling. Identical current review delivery is acknowledged without duplicate records/events; changed or revoked authority still fails closed. All artifacts enter as unverified and there is no production verification path, so task completion remains blocked until trusted sandbox attestation exists. Worker identity registration, independent reviewer invocation and real execution remain open. See [artifact review integration requirements](AI_ARTIFACT_REVIEW_CONTRACT.md).

The full roadmap is not complete. Phases 0–3 remain partial: live authenticated inference, deployed database acceptance, independent review, runtime identity integration, cloud-provider routing and eight-hour soak testing remain open. Objective/plan and run/event pagination are implemented. Attention, basic requester digests and copied-context retention are partial operations additions, not completion of Phase 5; live email verification, broader retention and operations remain open. Phase 4 has artifact/review/acceptance groundwork, but no authorized execution host or running sandbox. No simulated coding results are presented as real execution.

Unauthenticated model discovery succeeded earlier. It does not verify authenticated generation, billing or execution access. The user reports adding `NUCLEAS_AI_REMOTE_BEARER_TOKEN` to Vercel; it has not been read or tested by this implementation.

## Verification

### September 9 authenticated execution-probe preparation

Added a separately authorized one-time administrator diagnostic for the existing remote Responses endpoint. It accepts no arbitrary code or destination, retains an attempted marker before sending, shares dispatcher limits, caps output/response size, and returns only sanitized outcomes. It does not enable coding execution or mark artifacts verified. All **726 tests across 115 files** pass, including concurrent probe admission and API/transport safeguards; targeted lint passes. See [diagnostic behavior and limits](AI_EXECUTION_PROBE.md). Live outcome is not established by these mocked tests.

### September 8 artifact inspection and review replay follow-up

Final production build passes, including TypeScript and 116 generated pages. Targeted lint and diff whitespace checks pass. Existing middleware/Browserslist warnings remain; changes are uncommitted and the original roadmap is unchanged.

The full suite passes **715 tests across 113 files**, including 74 isolated database integration tests. New tests cover identical concurrent review replay without duplicate events, changed payload conflicts, revoked grants, cancelled runs, scoped patch/evidence access, stored-byte tampering, 16,384-character pagination, binary decoding rejection and private authenticated content responses. The first evidence-reader test exposed a MongoDB parent/child projection collision; selecting explicit digest/byte subfields fixed it. No live provider, sandbox, production database or secret was used. Browser interaction and rich diff formatting remain unverified; the viewer renders bounded plain text rather than a syntax-highlighted diff.

### September 8 durable identity/review implementation

Full regression suite passes: **706 tests across 112 files**, including 70 temporary-replica-set integration tests. Added coverage includes hashed credentials and private audits, rotation/revocation, grant scope/expiry, issuer role loss (including after review and before acceptance), run cancellation/pauses, unverified acceptance denial, failed review, concurrent acceptance once, assignment preservation, tampered bytes, changed task state and organization isolation. Three API test suites cover administrator identity/grant controls and bearer-authenticated review submission. Successful acceptance tests simulate verified execution only by a guarded test-only database write; no production attestation claim is made. All 70 integration tests also pass after the final authorization-denial response mapping.

Final production build passes, including TypeScript and all 116 generated pages. Targeted lint passes for the new identity/grant controls, service review routes, storage/query models and acceptance modules; diff whitespace checks pass. Existing middleware-deprecation and stale Browserslist-data warnings remain. These checks do not replace browser interaction, live-provider acceptance, sandbox denial tests or the eight-hour soak. The prior light-usage checkpoint below predates this feature batch. All changes remain uncommitted; the original roadmap and unrelated `eslint-err.log` are untouched.

### September 8 light-usage and baseline follow-up

- Default page-activity consumers now share one subscribed visibility/idle store instead of installing a timer and listeners per view. Hidden pages have no idle timer, the last unsubscribe releases all resources, and frequent input extends the deadline without recreating timers or notifying unchanged snapshots. Custom idle durations retain isolated stores.
- Calendar progress labels display their current value directly, removing per-card 16 ms number-animation intervals. Entrance animations disconnect after first visibility and cancel pending reveal timers on cleanup; browsers without IntersectionObserver reveal immediately.
- Portal readiness uses a shared SSR-safe hook without individual mount effects. Image previews and content-create dialogs use balanced shared scroll locks, preserving overlapping overlays and restoring prior overflow styles. Multi-select remains closed during SSR and synchronizes its value ref after commit.
- Analytics/window/workspace refs are updated after commit instead of during render. The voice indicator uses deterministic decorative bar heights. JSX punctuation escaping preserves visible login/privacy/terms copy; no policy wording was changed.
- Voice/palette confirmation now consumes its pending action synchronously before invoking execution, so repeated confirmation cannot reuse that slot while a request is pending. Cancellation and slot edits synchronize immediately. This is client-side duplicate protection, not a substitute for server authorization or mutation idempotency.
- No production settings, secrets, deployments, remote inference calls or worker provisioning were performed. These changes are not an eight-hour soak, browser-interaction verification, provider benchmark or independent security review. The original roadmap remains unchanged.

Latest regression run: **670 tests across 109 files pass**, including 18 new lifecycle, shared-idle, SSR-readiness, scroll-lock and confirmation-consumption tests. Full TypeScript passes. The latest full lint scan reports **47 errors / 229 warnings** (original baseline 80 / 230; preceding checkpoint 78 / 229). Resolving render-ref diagnostics exposed additional pre-existing effect/memoization findings in WorkspaceShell; these were retained, not suppressed. Remaining lint failures primarily require behavioral review of state synchronization and effects. Dependency remediation, browser interaction/soak checks, live-provider acceptance, cloud routing and durable review/service-identity integration remain open; remote execution remains gated on a separately authorized sandbox host.

Final September 8 verification: production build passes against the final source state, including TypeScript and all 115 generated pages. Targeted lint for the new lifecycle/confirmation modules and the confirmation provider passes; `git diff --check` passes. No lint configuration or suppression changes were made. All work remains uncommitted, and the unrelated `eslint-err.log` is untouched.

### Earlier verification checkpoints

- 652 tests passed across 104 files, including 55 integration tests against a temporary local MongoDB replica set with mocked inference and email. These cover actual transactions, concurrency, settings persistence/auditing, scoped ceilings and monthly history, organization/project pauses, budget rollback, cancellation, lease recovery, approval preserving human assignments, history pagination, attention visibility/revocation, private acknowledgments, bounded scans, private-field exclusion, shared inference throttling/rollback, administrator usage snapshots, bounded diagnostics, terminal context retention, notification delivery/revocation, overlapping digest workers, artifact-review contracts and scoped service grants. An earlier concurrent build/test run timed out in one existing database test; the latest full suite ran separately from the build and passed without changing its timeout. Browser interaction and eight-hour memory/CPU behavior remain unverified.
- Production build passed. Targeted ESLint has no errors; the existing workspace notification module reports three unused-code warnings.
- Baseline before implementation: 374 tests/76 files; 81 existing TypeScript test-fixture diagnostics; full ESLint 80 errors/230 warnings. TypeScript checking now passes; the full ESLint baseline remains unresolved.
- A focused test-only fixture cleanup reduces TypeScript diagnostics from 81 to 69: supported Vitest environment stubs, concrete task IDs/status/date inference, and complete synthetic billing-plan documents. Production types are unchanged; no suppression directives were added. All 652 tests still pass and targeted lint is clean. The production build last passed before this test-only batch; it was not rerun for these fixture edits. Remaining fixture diagnostics are still open.
- A subsequent client/project fixture batch reduces TypeScript diagnostics from 69 to 46. Client impact reports, hub resolution and client project helpers now use synthetic model documents and real ObjectIds instead of incompatible partial-document assertions. All 652 tests across 104 files pass again; targeted lint and diff checks pass. Production code and types are unchanged, and no database connection is needed by these fixtures. The production build was not rerun for this test-only batch.
- The final fixture batch reduces the remaining 46 TypeScript diagnostics to zero (`tsc --noEmit --pretty false` passes). Calendar, scheduling, recency, hours, assignment-filter and notification fixtures now use model-backed documents, valid ObjectIds and appropriate date/status values. Recency tests retain explicit coverage for legacy tasks without IDs. Production types and code remain unchanged; no suppression directives were added. All 652 tests across 104 files pass, including 55 isolated database integration tests; targeted ESLint and diff checks pass. Production build was not rerun for these test-only edits. Earlier diagnostic counts above document intermediate checkpoints, not current failures.
- A focused mobile navigation lint batch removes the conditional `useMobileShell` call after the marketing-page return and aligns menu callback dependencies with the captured shell context. Both changed components pass targeted ESLint. Full TypeScript, all 652 tests/104 files, production build (including 115 generated pages), and diff checks pass. Full ESLint now reports 78 errors and 229 warnings, down from the original 80/230 baseline; no lint rules were disabled. Browser route transitions and mobile interactions remain unverified. Build warnings still include the middleware convention deprecation and stale Browserslist data. This is a baseline correction, not completion of the roadmap's UI/performance release gates.
- Dependency audit currently reports 46 vulnerabilities (3 low, 33 moderate, 10 high); no broad dependency remediation was attempted.
- Integration tests download/cache a MongoDB binary on first execution and remove the temporary database afterward. They do not call a model or production database.

Browser interaction, live authenticated inference, deployed database acceptance and soak tests have not run. No production environment flags or secrets were changed. The unrelated `eslint-err.log` remains untouched.

See [deployment instructions](AI_PLANNING_DEPLOYMENT.md) for the remaining configuration and release check.
